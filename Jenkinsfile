pipeline {
  agent any
  stages {
    stage('Etapa commit') {
      steps {
        echo 'Hola, clase desde Github!'
      }
    }
    stage('Etapa aceptación') {
      steps {
        echo 'Hola, clase desde la etapa de aceptación!'
      }
    }
  }
}
